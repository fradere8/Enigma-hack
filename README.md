# 🚀 Автоматизированное рабочее место Технической поддержки (АРМ ТП)

Решение тестового задания для кейса «Интеллектуальная обработка обращений».
Проект представляет собой скелет системы для автоматизации разбора входящей почты, классификации заявок с помощью ML и интерфейса для работы оператора.

---

## 👥 Команда: [Название команды]

*   **Иванов Иван** — Team Lead / Backend (архитектура, API)
*   **Петров Петр** — Data Scientist (NLP, промпт-инжиниринг)
*   **Сидоров Сидор** — Frontend Developer (интерфейс, UX/UI)
*   **Смирнова Анна** — Designer / Analyst (бизнес-логика, дизайн)

**Контакты:** [email/telegram тимлида]

---

## 🛠 Технический стек

*   **Backend:** Python 3.11, FastAPI (асинхронная обработка), SQLAlchemy.
*   **Frontend:** React (Vite), TailwindCSS / Ant Design (для таблиц).
*   **Database:** PostgreSQL 15.
*   **AI/ML (План):** 
    *   *Классификация и тональность:* RuBERT-tiny2 (быстрая и легкая).
    *   *Извлечение сущностей (NER):* SpaCy / Natasha.
    *   *Генерация ответов:*  Open Source LLM через Ollama.
*   **DevOps:** Docker, Docker Compose.

---

## ⚙️ Архитектура решения

Мы разделили систему на независимые микросервисы для удобства масштабирования на хакатоне:

1.  **Email Poller Service:** Фоновый воркер (Celery/APScheduler). Подключается по IMAP к почте, проверяет новые письма каждые N минут и сохраняет «сырые» данные в БД.
2.  **Analysis Service:** Забирает необработанные письма. 
    *   Парсит тело письма.
    *   Определяет тональность (Positive/Neutral/Negative).
    *   Выделяет сущности (ФИО, Заводские номера, Организация).
    *   Формирует краткую суть (Summary).
3.  **Backend API:** Предоставляет REST API для фронтенда.
4.  **Frontend:** Дашборд оператора. Позволяет сортировать заявки, редактировать некорректно распознанные поля и отправлять ответы.

---

## 🗺 Пользовательский путь (User Journey)

1.  Клиент отправляет письмо на support@...
2.  Система автоматически создает заявку в БД со статусом `New`.
3.  Оператор видит новую строку в таблице.
    *   *Красная подсветка* — клиент зол (отрицательная тональность), требуется срочный ответ.
    *   *Зеленая подсветка* — благодарность или простой вопрос.
4.  Оператор открывает карточку заявки. Система уже сгенерировала **черновик ответа** на основе базы знаний.
5.  Оператор проверяет заводские номера и текст ответа, при необходимости правит.
6.  Нажатие кнопки «Отправить»: письмо уходит клиенту по SMTP, статус меняется на `Closed`.

---

## ⚠️ Анализ рисков и решения

| Риск | Вероятность | Решение в нашем проекте |
| :--- | :--- | :--- |
| **Галлюцинации LLM** (неверный технический совет) | Средняя | Реализован принцип **Human-in-the-loop**: ответ не уходит автоматически, оператор всегда должен подтвердить отправку. |
| **Ошибки парсинга** (неверно извлекли номер телефона) | Высокая | Поля в интерфейсе сделаны редактируемыми. Оператор может вручную скорректировать данные перед сохранением. |
| **Дубликаты заявок** | Средняя | Проверка по `Message-ID` письма. Если клиент ответил на то же письмо, ветка переписка подтягивается к существующей заявке. |

---

## 🚀 Инструкция по запуску

Для запуска требуется установленный **Docker** и **Docker Compose**.

```
.
├── backend/            # Исходный код API (FastAPI)
│   ├── app/
│   │   ├── models/     # Модели БД (SQLAlchemy)
│   │   ├── schemas/    # Pydantic схемы
│   │   ├── services/   # Логика ML и почты
│   │   └── main.py
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/           # Исходный код интерфейса (React)
│   ├── src/
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml  # Оркестрация контейнеров
└── README.md
```

